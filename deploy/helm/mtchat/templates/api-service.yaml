apiVersion: v1
kind: Service
metadata:
  name: {{ include "mtchat.fullname" . }}-api
  labels:
    {{- include "mtchat.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mtchat.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
